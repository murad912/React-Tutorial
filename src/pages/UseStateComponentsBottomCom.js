import React from 'react';

export const BottomComponents = ({ message }) => {
    let content =
        <div>
            <p>
                {message}
            </p>
        </div>
    return content;
}